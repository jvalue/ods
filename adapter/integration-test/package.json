{
  "name": "adapter-service-it",
  "version": "1.0.0",
  "description": "Adapter-Service Integration-Tests",
  "scripts": {
    "mock": "node src/mock.server.js",
    "pretest": "npm run mock &",
    "test": "jest",
    "posttest": "killall node"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "koa": "^2.7.0",
    "koa-router": "^7.4.0",
    "wait-on": "^3.2.0"
  }
}
